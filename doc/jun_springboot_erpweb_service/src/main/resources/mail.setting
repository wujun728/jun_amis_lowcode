# 邮件服务器的SMTP地址，可选，默认为smtp.<发件人邮箱后缀>
host = smtp.qq.com
# 邮件服务器的SMTP端口，可选，默认25
port = 25
# 发件人（必须正确，否则发送失败）
from = nb666@foxmail.com
# 用户名，默认为发件人邮箱前缀
user = 1692700664
# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码，详情查看相关帮助）
# pass = 授权码   一下授权码是我自己的 你们可以测试使用
pass = iguuwmpwstwqdjfh